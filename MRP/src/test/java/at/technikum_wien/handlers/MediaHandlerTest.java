package at.technikum_wien.handlers;

import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;

import static org.mockito.Mockito.*;

public class MediaHandlerTest {

    @BeforeAll
    static void setUp() {
        MediaHandler mediaHandler;
    }

    @Test
    void handleSearchMedia(){
        //arrange

        //act

        //assert
        // GOAL: verify(mediaHandler.handleSearchMedia,times(1),).add(exchange,query);
    }
}
